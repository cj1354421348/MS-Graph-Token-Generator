# Azure AD App Registration Details
# 必须配置，否则应用无法启动
CLIENT_ID=your_client_id_here

# 可选：如果注册为 Web 应用 (Confidential Client)，必须填写
# 如果注册为 桌面/移动应用 (Public Client)，请留空或删除
# CLIENT_SECRET=your_client_secret_here

# Flask App Configuration
# 生产环境建议设置
FLASK_SECRET_KEY=generate_a_strong_random_key_here

# Redirect URI
# 对于桌面/移动应用 (推荐)，请在 Azure 注册 http://localhost
# 本地代码会自动处理 http://localhost:5000/ (根路径)
REDIRECT_URI=http://localhost:5000

# Scope (Permissions)
# 注意：不能将 User.Read (Graph) 与 Outlook Scopes 混合使用，否则会报 AADSTS70011
# "读写邮件全上" -> IMAP + SMTP
SCOPE=offline_access https://outlook.office.com/IMAP.AccessAsUser.All https://outlook.office.com/SMTP.Send

# Server Configuration
# 默认端口 5000，默认 host 0.0.0.0
PORT=5000
HOST=0.0.0.0
FLASK_DEBUG=True
DB_URL=postgresql://user:password@host/dbname?sslmode=require
